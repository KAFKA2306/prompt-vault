interface:
  display_name: "Prompt Vault Update"
  short_description: "Register artifacts and update the prompt DB through canonical validators"
  default_prompt: "Follow AGENTS.md and this skill. Use scripts/artifacts/register_generated_artifact.py for artifact registration, existing validators for verification, and fail closed on unverified state."
policy:
  allow_implicit_invocation: true
