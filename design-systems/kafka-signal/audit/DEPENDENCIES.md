# Issue dependency

- #24 discovers repositories and UI elements and fixes source paths/commits.
- Scoring engine converts evidence-backed proposed scores into an actionable scorecard.
- #25 may consume only `canonical_candidate=true` rows.
- #26 supplies/refreshes browser evidence used by the promotion gate.
- #31 re-runs the chain on changed UI only.
